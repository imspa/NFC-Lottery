package it.imwatch.nfclottery;

/**
 * A class that contains global constants for the app.
 */
public class Const {

    /**
     * Used to enable or disable the app's debug functions.
     * When set to true, the app produces a much more verbose log output.
     */
    public static final boolean DEBUG = BuildConfig.DEBUG;

}
